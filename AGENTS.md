<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## UI/UX skills

- Use `ui-ux-pro-max` for every UI/UX design, implementation, review, and polish task.
- Use `framer-motion` whenever UI work includes motion, transitions, gestures, or micro-interactions.
- Use `21st-ai` for new page, section, or component concepts and UI sketch generation. Do not spend paid iteration credits without explicit user approval.
- Use `21st-cli-use` to search the 21st.dev catalog before hand-writing UI components. Do not retrieve metered code or install catalog items without explicit user approval.

## TFM creative direction

- Follow the global Anti-Generic Creative Direction workflow before every website or funnel change.
- Treat `.ui-craft/brief.md` and `.ui-craft/tokens.md` as binding.
- Use only the official logo palette as the base. Beige and brass are not TFM brand colors.
- Keep the lead magnet and the gallery rhythm unless the user explicitly requests their removal.
- Use one restrained accent only for conversion actions and focus states.
